# 机车智能巡检机器人 知识库

> **生成时间：** 2026-07-29
> **数据来源：** `C:\Users\Administrator\Desktop\机车巡检机器人\资料\` 目录下全部文件
>

---

## 一、项目概述

### 1.1 项目名称
**机车智能巡检机器人系统**（型号：NCDC-D-A30）

本项目由**北铁所（北京铁道研究所）**与**国智中心（国家智能设计与数控技术创新中心）**联合开发，原计划部署于**南宁机务段**。系统集成了多模态传感器（海康威视相机、Zivid 3D相机、速腾激光雷达、RealSense双目相机、IMU）与双机械臂，搭载于 NVIDIA Jetson AGX Thor T5000 嵌入式AI计算平台。

#### 全国机务段巡检机器人部署概况

| 机务段 | 设计方 | 电控方案 | 与本项目关系 |
|--------|--------|----------|-------------|
| **南宁**（本项目） | 北铁所 + 国智中心 | **国智中心方案**（本知识库所述） | 本项目 |
| 怀化 | 北铁所 + 普华灵动 | 普华灵动方案 | 业务流程可参考 |
| 南昌 | 北铁所 + 普华灵动 | 普华灵动方案 | 业务流程可参考 |
| 杭州 | 北铁所 + 普华灵动 | 普华灵动方案 | 业务流程可参考 |
| 乌鲁木齐 | 北铁所 + 普华灵动 | 普华灵动方案 | 业务流程可参考 |

> **关键差异：** 国智中心与普华灵动的**电控方案有本质区别**（传感器选型、通信协议、控制架构均不同）。怀化/南昌/杭州/乌鲁木齐四地的资料在**巡检业务流程**上可参考，但**电控实现细节不可直接复用**。

### 1.2 核心硬件平台
| 组件 | 型号/规格 |
|------|-----------|
| **AI计算平台** | NVIDIA Jetson AGX Thor T5000（130W MAXN模式） |
| **系统环境** | Ubuntu 24.04 LTS + JetPack 7.1（L4T R38.4） |
| **CUDA** | CUDA 13.0 |
| **推理加速** | TensorRT 10.13.3.9 |
| **深度学习** | cuDNN 9.12.0 |
| **视觉处理** | VPI 4.0.5（Vision Programming Interface） |
| **多媒体** | L4T Multimedia API R38.4.0、L4T GStreamer |
| **视频编解码** | NVIDIA Video Codec SDK（Jetson） |
| **视频分析** | DeepStream 8.0（SAM2目标追踪） |
| **传感器采集** | Sensor Input Processing Library (SIPL) R38.4.0 |

### 1.3 传感设备清单

| 类别 | 设备 | 数量 | 接口 | 用途 |
|------|------|------|------|------|
| **工业相机** | 海康威视网络相机 | 多台 | 千兆以太网 (GigE) | 高清视频采集、视觉巡检 |
| **3D相机** | Zivid 2+ 精扫相机 | 1台+ | USB 3.0 | 高精度三维重建、零部件缺陷检测 |
| **3D相机** | Intel RealSense | 1台+ | USB 3.0 | 实时深度感知、彩色点云生成 |
| **激光雷达** | 速腾聚创 E1R | 1台+ | 以太网 | 环境扫描、360°点云建图 |
| **IMU** | CH10X系列 | 1个+ | 串口/I2C/SPI | 姿态解算、位姿估计 |
| **机械臂** | 大族Elfin系列 | 1台 | 以太网/EtherCAT | 末端执行器精准定位 |

---

## 二、开发环境配置（Linux/Jetson）

### 2.1 环境变量（来源 `.bashrc`）

```
CUDA_HOME=/usr/local/cuda-13.0
QT_SELECT=5
OpenCV_DIR=/usr/lib/cmake/opencv4
BOOST_ROOT=/usr/include/boost
EIGEN3_INCLUDE_DIR=/usr/include/eigen3
HIKVISION_SDK_PATH=/opt/hikvision_sdk_arm64
```

### 2.2 关键库路径

| 库 | 路径 | 版本 |
|----|------|------|
| CUDA | `/usr/local/cuda-13.0/lib64` | 13.0 |
| Qt5 | `/usr/lib/aarch64-linux-gnu/qt5/plugins` | 5.x |
| OpenCV | `/usr/lib/cmake/opencv4` | 4.8.0 (NVIDIA优化版) |
| PCL | 系统安装 | - |
| VTK | 系统安装 | 9.1 |
| Zivid | `/usr/lib/libZividCore.so` | 2.17.2 |
| Hikvision SDK | `/opt/hikvision_sdk_arm64` | V6.1.9.45 (ArmLinux64) |

### 2.3 开发和性能命令

```bash
# 启用MAXN性能模式 (130W)
sudo nvpmodel -m 0 && sudo jetson_clocks

# 一键构建 (Qt5项目)
rm -rf build && mkdir build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release -DCMAKE_EXPORT_COMPILE_COMMANDS=ON && make -j$(nproc)

# 环境验证
env-check

# 海康SDK验证
hikvision-verify

# 查看日志
hikvision-log

# 3D相机捕获
zivid-capture

# RealSense预览
rsview
```

### 2.4 设备拓扑连接方式

- 海康相机 → GigE 交换机 → Jetson 千兆网口
- Zivid 2+ → USB 3.0 → Jetson
- RealSense → USB 3.0 → Jetson
- E1R 激光雷达 → 以太网 → Jetson（通过rslidar_sdk UDP接收或PCAP离线分析）
- CH10X IMU → 串口/USB → Jetson
- 大族Elfin机械臂 → 以太网/EtherCAT → Jetson（通过HuayanRobotV6协议）

---

## 三、海康威视 SDK（CH-HCNetSDK V6.1.9）

### 3.1 SDK 版本与路径

| 项目 | 值 |
|------|-----|
| 版本 | CH-HCNetSDK V6.1.9.48 build 20230410 |
| Linux ARM64 SDK | V6.1.9.45, 部署于 `/opt/hikvision_sdk_arm64` |
| 核心库 | `libhcnetsdk.so` → `HCCore.dll` + `HCCore.lib` |
| 组件目录 | `HCNetSDKCom/`（含`HCPreview`预览、`HCAlarm`报警、`HCPlayBack`回放等） |

### 3.2 主要头文件（位于 `头文件/`）

| 文件 | 行数 | 内容 |
|------|------|------|
| `HCNetSDK.h` | 52,612 | 主SDK头文件：所有设备管理API、数据类型、结构体、枚举常量声明 |
| `DataType.h` | 111 | 帧头定义、视频制式（NTSC/PAL/SECAM）、帧类型（I/P/BBP/Audio）、图像格式、OSD定义 |
| `plaympeg4.h` | 760 | PlayM4播放SDK：视频/音频解码播放、DirectDraw渲染、鱼眼矫正、VIE图像增强、YUV/RGB转换、PTZ控制、加密流支持 |
| `DecodeCardSdk.h` | 451 | DS-40xxHC/HF系列解码卡SDK：视频格式、码率控制、解码卡操作接口 |

### 3.3 核心功能模块

| 模块 | DLL | 功能 |
|------|-----|------|
| **设备管理** | `HCNetSDK.dll` | 设备注册、登录、参数配置、远程升级 |
| **实时预览** | `HCPreview.dll` / `PlayCtrl.dll` | 多窗口预览、截图、解码显示 |
| **录像回放** | `HCPlayBack.dll` | 远程回放、按时间/文件下载录像 |
| **报警管理** | `HCAlarm.dll` | 布防、撤防、报警回调（移动侦测/遮挡/视频丢失） |
| **语音对讲** | `HCVoiceTalk.dll` / `AudioIntercom.dll` | 双向语音对讲、音频转发 |
| **云台控制** | `HCIndustry.dll` | PTZ控制、预置位 |
| **远程配置** | `HCGeneralCfgMgr.dll` | 远程参数配置 |
| **透传通道** | `StreamTransClient.dll` | ISAPI协议透传 |
| **解码显示** | `HCDisplay.dll` | 视频解码渲染 |
| **维护管理** | `HCCoreDevCfg.dll` | 设备维护、日志导出 |

### 3.4 开发示例（Windows）

| 语言 | 示例数量 | 覆盖功能 |
|------|---------|----------|
| **C++ (MFC)** | 1个综合示例 + 8个分功能示例 | 实时预览、抓图、YUV获取、回放下载、远程配置、报警布防、语音对讲、透传通道、维护管理 |
| **C#** | 8个示例 | 实时预览（2种）、录像下载回放、报警布防、图片下载、远程参数配置、视音频解码回调、文件下载回放 |
| **Java** | 11个示例 | 预览回放下载、报警布防监听、语音对讲转发、视频无极缩放、文件下载回放、云台控制、ISAPI透传、JavaClientDemo |
| **Python** | 4个示例 | 预览取流保活、报警布防、回放下载、ISAPI协议透传 |

### 3.5 关键 API 调用序列

```
NET_DVR_Init()                           // 初始化SDK
  └─ NET_DVR_SetConnectTime()            // 设置超时
  └─ NET_DVR_Login_V40()                 // 登录设备
       ├─ NET_DVR_RealPlay_V40()         // 实时预览
       │    ├─ fRealDataCallBack()       // 码流回调
       │    └─ PlayM4_OpenStream()       // 解码（通过PlayCtrl）
       ├─ NET_DVR_SetUpAlarmChan_V41()   // 报警布防
       │    └─ MSGCallBack()             // 报警回调
       ├─ NET_DVR_PlayBackByTime()       // 按时间回放
       ├─ NET_DVR_VoiceComSendData()     // 语音对讲
       └─ NET_DVR_STDXMLConfig()         // ISAPI透传配置
  └─ NET_DVR_Logout()                    // 登出
NET_DVR_Cleanup()                        // 清理SDK
```

---

## 四、速腾聚创激光雷达 SDK

### 4.1 E1R 激光雷达规格

| 参数 | 说明 |
|------|------|
| 型号 | E1R（速腾聚创） |
| 手册 | `速腾 E1R手册.pdf`、`E1R产品手册.pdf` |
| 图纸 | `速腾 E1R图纸.pdf`（含机械安装尺寸） |
| 数据接口 | 以太网 UDP 点云流 |
| 配套工具 | LidarAssistant（上位机调试工具）、RSView（点云可视化） |

### 4.2 rslidar_sdk（v1.5.18）

**项目结构：**

```
rslidar_sdk/
├── CMakeLists.txt           # 主构建（C++17, POINT_TYPE=XYZI）
├── package.xml              # ROS包描述
├── launch/start.launch      # 启动文件
├── config/                  # YAML配置文件
├── doc/                     # 文档（含howto/intro/src_intro）
├── src/
│   ├── manager/node_manager.cpp      # 数据源管理与分发
│   ├── msg/                           # ROS消息定义
│   └── rs_driver/                     # 核心驱动（v1.5.18）
│       ├── CMakeLists.txt             # C++14, 跨平台
│       ├── demo/
│       │   ├── demo_online.cpp        # 在线实时采集
│       │   ├── demo_pcap.cpp          # PCAP离线回放
│       │   └── demo_online_multi_lidars.cpp  # 多雷达
│       ├── tool/
│       │   ├── rs_driver_viewer.cpp   # PCL点云可视化
│       │   └── rs_driver_pcdsaver.cpp # PCD文件保存
│       └── src/                        # 驱动核心源码
└── node/rslidar_sdk_node.cpp           # ROS节点入口
```

**支持雷达型号（17种）：** RS16, RS32, RSBP, RS128, RS80, RSM1, RSHELIOS, RSM2, RSHELIOS_16P, BPEARL, RSAIRY, RSFAIRY, RSEMX, RSEMX_COMP, RSMX, RSDAIRY

**构建依赖：** C++14/C++17, CMake 3.5+, PCL (可选), Eigen3 (可选), ROS1/ROS2 (可选), PCAP (可选)

### 4.3 RSView 点云可视化工具

**版本：** RSView_Win 4.3.15 (基于 ParaView/VTK/PCL)

| 文件 | 说明 |
|------|------|
| `RSView.exe` | 可执行程序 |
| `rsview/applogic.py` (3203行) | 核心应用逻辑 |
| `rsview/planefit.py` | RANSAC平面拟合（标定用） |
| `rsview/timer.py` | FPS帧率计时器 |
| `rsview/gridAdjustmentDialog.py` | 测量网格调整 |
| `rsview/kiwiviewerExporter.py` | KiwiViewer 3D导出 |
| `rsview/__init__.py` | ParaView路径检测 |

**支持雷达类型：** RSAIRY, RSFAIRY, RSEMX, RSEMX_COMP（支持列/环着色、传感器位置调整、PCAP/CSV/PCD/LAS导入导出）

### 4.4 LidarAssistant 调试工具

**版本：** v1.4.1.0（20250326客户版本）

| 平台 | 路径 |
|------|------|
| Windows | `工具/windows/LidarAssistant.exe` |
| Linux | `工具/linux/LidarAssistant` |
| 配置 | `Config.ini`（Project=E1, CommunicationMode=eth） |
| 支持项目 | E1, E1_Asample |

**依赖：** Qt5 (Core/Gui/Network/Widgets/SerialBus/SerialPort), WinPcap (wpcap.dll), Kvaser (canlib32.dll)

---

## 五、Intel RealSense SDK

### 5.1 SDK 文档

| 文件 | 内容 |
|------|------|
| `Intel® RealSense™ SDK：C++ API完全参考手册-CSDN博客.pdf` | C++ API 完整参考 |
| `realsense python 生成点云_mob64ca13f48509的技术博客_51CTO博客.pdf` | Python 生成彩色点云教程 |

### 5.2 点云生成流程（Python）

```python
import pyrealsense2 as rs
import numpy as np
import open3d as o3d

pipeline = rs.pipeline()
config = rs.config()
config.enable_stream(rs.stream.depth, 640, 480, rs.format.z16, 30)
config.enable_stream(rs.stream.color, 640, 480, rs.format.bgr8, 30)
pipeline.start(config)

align = rs.align(rs.stream.color)
frames = pipeline.wait_for_frames()
aligned_frames = align.process(frames)

depth_frame = aligned_frames.get_depth_frame()
color_frame = aligned_frames.get_color_frame()

pc = rs.pointcloud()
pc.map_to(color_frame)
points = pc.calculate(depth_frame)

# 转 numpy 数组
vtx = np.asanyarray(points.get_vertices()).view(np.float32).reshape(-1, 3)
tex = np.asanyarray(points.get_texture_coordinates()).view(np.float32).reshape(-1, 2)
```

### 5.3 C++ API 关键组件

| 组件 | 功能 |
|------|------|
| `rs2::pipeline` | 流管道管理 |
| `rs2::config` | 流配置（分辨率、帧率、格式） |
| `rs2::align` | 深度-彩色对齐 |
| `rs2::pointcloud` | 点云计算 |
| `rs2::context` | 设备枚举 |
| `rs2::device` | 设备控制 |
| `rs2::sensor` | 传感器控制（曝光、增益等） |

---

## 六、Zivid 3D 相机 SDK

### 6.1 文档清单

| 文件 | 内容 |
|------|------|
| `Zivid_C++_Namespace.pdf` | C++ 命名空间结构参考 |
| `Zivid_C++_Classes.pdf` | C++ 类层次结构参考 |
| `Zivid Studio User Guide SDK 2.13 - Chinese.pdf` | Zivid Studio 使用指南（SDK 2.13） |
| `Zivid Studio安装及使用教程 (2).pdf` | Zivid Studio 安装教程 |
| `Zivid 2+ User Guide 1.8 - Chinese.pdf` | Zivid 2+ 相机用户手册 |
| `zivid精扫相机技术协议20240627.pdf` | 技术协议（采购） |
| `ZIVID 3D相机_昊艾报价单.pdf` | 报价单 |

### 6.2 SDK 核心信息

| 项目 | 值 |
|------|-----|
| SDK 版本 | 2.17.2 (Jetson上部署) |
| 核心库 | `libZividCore.so` |
| 采集参数 | ExposureTime: 8000μs, Gain: 1.0 |
| 输出格式 | ZDF (Zivid Data File) |
| 一键采集命令 | `ZividCapture --settings '{"Acquisitions": [{"ExposureTime": 8000, "Gain": 1.0}]}' --output /tmp/capture.zdf` |

---

## 七、大族机器人机械臂

### 7.1 文档清单

| 文件 | 内容 |
|------|------|
| `大族机械臂Elfin软件操作说明书V6.4.pdf` | Elfin系列软件操作说明（含示教器操作、程序编写） |
| `Elfin软件操作说明书V6.4.pdf` | 同上（备份/另版） |
| `HuayanRobotV6控制通信协议接口_V1.0.19.1.pdf` | 华研机器人V6通信协议（TCP/IP Ethernet接口） |
| `Mini Control Box 使用说明书20220513版.pdf` | 迷你控制箱硬件安装与接线 |

### 7.2 通信协议

- **控制器：** HuayanRobot V6
- **协议版本：** V1.0.19.1
- **物理接口：** 以太网 TCP/IP
- **功能：** 关节角度控制、笛卡尔坐标控制、状态查询、运动规划、安全急停

---

## 八、IMU 惯性测量单元

### 8.1 文档清单

| 文件 | 内容 |
|------|------|
| `ch10x_datasheet_cn.pdf` | CH10X系列 IMU 数据手册（电气参数、性能指标） |
| `CH10X_um_cn(1).pdf` | CH10X系列 IMU 用户手册（通信协议、配置方法） |
| `IMU配置手册.pdf` | IMU配置与标定手册 |

---

## 九、NVIDIA Jetson 平台

### 9.1 系统与驱动

| 资源 | 版本 | 文件 |
|------|------|------|
| **L4T BSP** | R38.2.1 / R38.4.0 | `Jetson_Linux_R38.x_aarch64.tbz2` |
| **SIPL** | R38.2.1 / R38.4.0 | `Jetson_SIPL_API_R38.x_aarch64.tbz2` |
| **示例根文件系统** | R38.2.1 / R38.4.0 | `Tegra_Linux_Sample-Root-Filesystem_R38.x_aarch64.tbz2` |
| **JetPack** | 7.1 | SDK清单: `sdkml3_jetpack_71_win_locale.json` |

### 9.2 AI 加速 SDK（Debian包，ARM64）

| SDK | 版本 | 文件 |
|-----|------|------|
| **CUDA** | 13.0 | `cuda-repo-ubuntu2404-13-0-local_13.0.0-580.65.06-1_arm64.deb` |
| **cuDNN** | 9.12.0 | `cudnn-local-repo-ubuntu2404-9.12.0_1.0-1_arm64.deb` |
| **TensorRT** | 10.13.3.9 | `nv-tensorrt-repo-ubuntu2404-cuda13.0-trt10.13.3.9-sbsa-target-ga-20250825_1-1_arm64.deb` |
| **DeepStream** | 8.0 | `deepstream-8.0_8.0.0-1_arm64.deb` |
| **Graph Composer** | 5.1.0 | `graph_composer-runtime-5.1.0_arm64.deb` |

### 9.3 视觉加速 SDK（ARM64）

| SDK | 版本 | 文件 |
|-----|------|------|
| **OpenCV** | 4.8.0 | `OpenCV-4.8.0-*-aarch64-*.deb`（含 dev/libs/licenses/python/samples，共10个包） |
| **VPI** | 多个版本 | `vpi-dev-*.deb`, `vpi-lib-*.deb`, `vpi-python-*.deb`, `vpi-samples-*.deb` |
| **L4T Multimedia API** | 两个版本 | `nvidia-l4t-jetson-multimedia-api_*.deb` |
| **L4T GStreamer** | 两个版本 | `nvidia-l4t-gstreamer_*.deb` |
| **Video Codec SDK** | - | `nvidia-video-codec-sdk_*.deb` |

### 9.4 其他工具链

| 工具 | 文件 |
|------|------|
| **PVA SDK** | `pva-sdk-*-l4t_*.deb`（可编程视觉加速器） |
| **Nsight Systems** | `nsight-systems-*.deb`（性能分析） |
| **CUDA Debugging Support** | `nvidia-l4t-cudadebuggingsupport_*.deb` |
| **NVIDIA Container Toolkit** | `nvidia-container-toolkit*.deb`, `libnvidia-container*.deb` |
| **SDKM** | `Nvidia_SDKM_Ubuntu_2204.tar` |

---

## 十、铁路施工与运维文档

| 文件 | 内容 |
|------|------|
| `北铁所南昌机务段铁轨及二维码施工说明2025年9月16日.pdf` | 南昌机务段铁轨区域施工说明和二维码标识布置方案 |
| `国智中心技术开发合同_机车智能巡检机器人系统-终稿.doc` | 国家智能设计与数控技术创新中心技术开发合同 |
| `机车采集软件总框架20250723.pdf` | 采集软件整体架构设计文档 |
| `机车机器人设备拓扑图20251121.pdf` | 系统设备拓扑连接图 |
| `机车巡检机器人_[NCDC-D-A30]_20260311(1).pdf` | NCDC-D-A30型巡检机器人系统集成手册 |

---

## 十一、双目相机与 PCL 教程

| 文件 | 内容 |
|------|------|
| `双目相机与PCL实战：从RGB图像到彩色点云（PointXYZRGB）的完整流程-CSDN博客.pdf` | 完整的双目相机→RGB图像→视差图→PCL彩色点云转换流程教程 |
| `QSerialPort.pdf` | Qt5 SerialPort 模块使用说明 |

### 11.1 PCL 点云关键类型

```
pcl::PointCloud<pcl::PointXYZ>      // XYZ 坐标点云
pcl::PointCloud<pcl::PointXYZRGB>   // XYZ + RGB 彩色点云
pcl::PointCloud<pcl::PointXYZI>     // XYZ + Intensity 强度点云 (rslidar_sdk 默认)
pcl::PointCloud<pcl::PointXYZRGBNormal> // XYZ + RGB + 法向量
```

---

## 十二、采集软件总框架（核心架构）

基于 `.bashrc` 和各SDK信息推断的系统总体架构：

```
┌─────────────────────────────────────────────────┐
│              机车采集软件层                       │
│  Qt5 GUI / 控制面板 / 点云可视化 / 数据管理         │
└────────────────────┬────────────────────────────┘
                     │
     ┌───────────────┼───────────────┐
     │               │               │
┌────▼────┐   ┌─────▼──────┐  ┌─────▼──────┐
│ 3D感知   │   │  2D 视觉   │  │  运动控制   │
│ 模块     │   │  模块      │  │  模块      │
├─────────┤   ├────────────┤  ├────────────┤
│ E1R雷达  │   │ 海康相机   │  │ Elfin机械臂 │
│ Zivid    │   │ SDK V6.1.9 │  │ V6协议      │
│ RealSense│   │            │  │ IMU CH10X  │
│ PCL配准  │   │            │  │            │
└────┬─────┘   └─────┬──────┘  └─────┬──────┘
     │               │               │
     └───────────────┴───────────────┘
                     │
         ┌───────────▼───────────┐
         │   NVIDIA Jetson Thor  │
         │   JetPack 7.1         │
         │   CUDA 13.0 / TensorRT│
         │   PCL / VTK / OpenCV  │
         └───────────────────────┘
```

---

## 十三、关键代码路径映射

### 13.1 海康威视 SDK 集成

```cpp
// 头文件引用（Jetson上）
#include "HCNetSDK.h"       // 主SDK头文件
#include "plaympeg4.h"      // 播放SDK头文件
#include "DataType.h"       // 数据类型定义

// 库链接（CMakeLists.txt）
target_link_libraries(yourapp
    ${HIKVISION_SDK_PATH}/MakeAll/libhcnetsdk.so
    ${HIKVISION_SDK_PATH}/MakeAll/HCNetSDKCom/libHCCore.so
    # ... 其他组件库
)
```

### 13.2 速腾激光雷达集成

```cpp
// 直接使用 rs_driver（绕过ROS）
#include <rs_driver/driver/lidar_driver.h>

// 或者使用 rslidar_sdk（ROS方式）
roslaunch rslidar_sdk start.launch
```

### 13.3 点云处理管线

```
海康相机2D → OpenCV 预处理 → 检测识别
         ↓
Zivid 3D → PCL点云 → 缺陷检测 → 融合
         ↓
E1R雷达 → PCL点云 → 环境建图 → 定位导航
         ↓
RealSense → PCL点云 → 近距离细节
         ↓
IMU → 姿态融合 → 里程计
```

---

## 十四、文件统计

| 类别 | 数量 |
|------|------|
| PDF技术文档 | ~30+ 份 |
| 头文件 (.h) | 4 个 |
| C/C++ 源文件 | 50+ 个 |
| Python 源文件 | 10+ 个 |
| CMakeLists.txt | 5 个 |
| Shell 脚本 | 4 个 |
| JSON 配置 | 5 个 |
| 文本说明 (.txt) | 10 个 |
| Markdown 文档 | 8 个 |
| Debian 软件包 | 30+ 个 |
| DLL/SO 库文件 | 80+ 个 |
| **总计文件** | **~14,921 个** |
| **总计目录** | **703 个** |

---

## 附录：常用调试与开发命令速查

| 命令 | 用途 |
|------|------|
| `jetson-max` | 启用130W MAXN性能模式 |
| `jetson-status` | 查看Jetson硬件状态 |
| `jetson-temp` | 查看GPU/CPU温度 |
| `env-check` | 完整环境验证（含海康SDK检测） |
| `hikvision-verify` | 验证海康SDK是否就绪 |
| `hikvision-log` | 查看SDK日志 |
| `qtbuild` | Qt项目一键构建 |
| `zivid-capture` | Zivid快速采集 |
| `rsview` | 启动RealSense Viewer |
| `opencv-version` | 查看OpenCV版本 |
| `vtk-version` | 查看VTK版本 |

---

---

## 十五、Collector_Manage 采集管理软件系统

> **源码路径：** `C:\Users\Administrator\Desktop\机车巡检机器人\机车巡检机器人\Collector_Manage\`
> **平台：** NVIDIA Jetson AGX Thor (ARM64, Ubuntu 24.04, Qt5 + CUDA 13.0)
> **源码行数：** 主进程 ~2,241 行 + 6 个子进程 ~各 200-600 行 + 共享代码

### 15.1 系统架构：1主进程 + 6子进程

Collector_Manage 是机车型智能巡检机器人的**核心采集管理软件**，采用 **Qt5 多进程架构**，通过 IPC（进程间通信）协调各硬件传感器。

```
┌────────────────────────────────────────────────────────────┐
│              Collector_Manage (主进程, Qt GUI)              │
│   QMainWindow + 20+ QThread + QProcess × 7 子进程管理器     │
└──────┬──────┬──────┬──────┬──────┬──────┬─────────────────┘
       │      │      │      │      │      │
  ┌────▼──┐ ┌▼───┐ ┌▼───┐ ┌▼───┐ ┌▼───┐ ┌▼──────┐
  │Zivid  │ │Real │ │Robo │ │Hikv │ │Trans│ │HIPNUC │
  │Camera │ │Sense│ │Sense│ │ision│ │_Svr │ │_CH10X │
  │3D采集 │ │D555 │ │E1R  │ │相机 │ │文件  │ │IMU   │
  │       │ │深度 │ │雷达 │ │SDK  │ │传输  │ │惯导  │
  └───────┘ └────┘ └─────┘ └─────┘ └─────┘ └───────┘
```

### 15.2 CMake 构建系统

**根 `CMakeLists.txt`** (`Collector_Manage/CMakeLists.txt:67`)

- C++17 + CUDA（CMAKE_CUDA_ARCHITECTURES=120）
- Qt5 AUTOMOC/AUTOUIC/AUTORCC
- 统一输出目录：`${CMAKE_BINARY_DIR}/bin/`
- 7 个子项目：

| 子项目 | 可执行文件 | 用途 |
|--------|-----------|------|
| `Collector_Manage/` | `collector_manage` | 主进程 GUI 调度器 |
| `Zivid_Camera/` | `zivid_manage` | Zivid 3D 相机采集 |
| `RealSense/` | `realsense_manage` | Intel RealSense D555 |
| `RoboSense/` | `robosense_manage` | 速腾 E1R 激光雷达 |
| `Hikvision/` | `hikvision_manage` | 海康威视监控相机 |
| `Transmission_Server/` | `transmission_server` | 双端口 TCP 文件传输 |
| `HIPNUC_CH10X/` | `hipnuc_ch10x_manage` | CH10X IMU 惯导 |

**运行路径：** `/home/nvidia/Collector_Manage/build/bin/{binary}`

### 15.3 进程间通信（IPC）机制

主进程与子进程之间通过 **QProcess stdin/stdout/stderr** 进行通信：

```
主进程 (QProcess)                    子进程
     │                                 │
     ├─ write("Connect") ───────────→ stdin
     │                                 ├─ readMessage("Connect")
     │                                 ├─ 连接硬件设备
     │                                 └─ write("Connect_Done") → stdout
     │  ← Read_Data() 解析 "Connect_Done"
     │                                 
     ├─ write("Disconnect") ────────→ stdin  
     │  ← "Disconnect_Done"
     │
     │  ← "Run&{winId}" ─────────── 子进程窗口已创建
     │     主进程通过 QWindow::fromWinId() 
     │     嵌入到 QWidget 中显示
```

**关键实现文件：**
- `Process_Remote.h/cpp` — **父类**，所有子进程远程管理的基类
- `Zivid_Remote.h/cpp` — Zivid 子进程专用管理
- `E1R_Remote.h/cpp` — E1R 激光雷达子进程管理
- `RealSense_Remote.h/cpp` — RealSense 子进程管理
- `Hikvision_Remote.h/cpp` — 海康相机子进程管理
- `CH10X_Remote.h/cpp` — IMU 子进程管理

**Process_Remote 核心逻辑** (`Process_Remote.cpp:132`):

```cpp
// 进程退出自动重启（3秒后重连）
QObject::connect(m_Process, &QProcess::finished, [=]() {
    if (m_Start) {
        emit is_Stop();
        QTimer::singleShot(3000, this, &c_Process_Remote::Connect_Device);
    }
});

// stdout 解析：识别 Run&winId 协议
void Read_Data() {
    if (message == "Connect_Done") emit Connect_Done();
    if (message.split("&").at(0) == "Run") {
        WId windowId = message.split("&").at(1).toULong();
        emit Show(windowId);  // 通知主进程嵌入子窗口
    }
}
```

### 15.4 主进程 (Collector_Manage) 详解

**类：** `c_Collector_Manage` (`Collector_Manage.h:192`, `Collector_Manage.cpp:2241`)

#### 15.4.1 线程管理（20+ QThread）

| 线程变量 | 对象 | 端口/协议 | 功能 |
|----------|------|----------|------|
| `m_RGV_Remote_Thread` | `c_RGV_Remote` | Modbus TCP | RGV 轨道巡检车通信 |
| `m_Huayan_Monitor_120_Thread` | `c_Huayan_Monitor_120` | TCP 10005 | 左机械臂 监控端口 |
| `m_Huayan_Remote_120_Thread` | `c_Huayan_Remote_120` | TCP 10003 | 左机械臂 控制端口 |
| `m_Huayan_Monitor_121_Thread` | `c_Huayan_Monitor_121` | TCP 10005 | 右机械臂 监控端口 |
| `m_Huayan_Remote_121_Thread` | `c_Huayan_Remote_121` | TCP 10003 | 右机械臂 控制端口 |
| `m_Zivid_1_Thread` | `c_Zivid_Remote` | QProcess | Zivid 1号 3D相机 |
| `m_Zivid_2_Thread` | `c_Zivid_Remote` | QProcess | Zivid 2号 3D相机 |
| `m_E1R_1_Thread` | `c_E1R_Remote` | QProcess | E1R 1号 激光雷达 |
| `m_E1R_2_Thread` | `c_E1R_Remote` | QProcess | E1R 2号 激光雷达 |
| `m_RealSense_1_Thread` | `c_RealSense_Remote` | QProcess | RealSense 1号 D555 |
| `m_RealSense_2_Thread` | `c_RealSense_Remote` | QProcess | RealSense 2号 D555 |
| `m_Hikvision_Thread` | `c_Hikvision_Remote` | QProcess | 海康监控相机 |
| `m_Local_Remote_Thread` | `c_Local_Remote` | TCP Server | 本地控制服务（监听） |
| `m_Work_Remote_Thread` | `c_Work_Remote` | TCP + JSON-RPC | 一键巡检任务调度 |
| `m_Sql_Remote_Thread` | `c_Sql_Remote` | MySQL | 数据库写入 |
| `m_CH10X_Thread` | `c_CH10X_Remote` | QProcess | IMU 惯导传感器 |

#### 15.4.2 系统扫描循环 (10ms 周期)

```cpp
void System_Scan() {
    Huayan_120_Scan();  // 更新左臂关节/TCP位置/状态
    Huayan_121_Scan();  // 更新右臂关节/TCP位置/状态
    Zivid_1_Scan();     // 更新Zivid 1号状态
    Zivid_2_Scan();     // 更新Zivid 2号状态
    CH10X_Scan();       // 更新IMU姿态/里程计
    RGV_Scan();         // 更新RGV Modbus状态
    Work_Remote_Scan(); // 更新巡检任务状态
    
    QTimer::singleShot(10, [=]{ System_Scan(); }); // 递归，~100Hz
}
```

#### 15.4.3 机械臂双实例设计

系统管理 **两台** 华研/大族 Elfin 机械臂：
- **120（左臂）**: 控制端口 10003, 监控端口 10005
- **121（右臂）**: 控制端口 10003, 监控端口 10005

每台臂有 **3 个线程对象**：
1. `c_Huayan_Monitor_120` — 接收 Datasheet 数据（位置/速度/IO/错误码）
2. `c_Huayan_Remote_120` — 发送控制命令（使能/断电/脚本/急停/复位/回零）
3. `c_Scan_Server_120` — 精扫协调（TCP Server，接收机械臂 PHOTO/START/FINISH 指令）

**支持的控制指令：**

| 指令 | 方法 | 说明 |
|------|------|------|
| 上电 | `Electrify()` | 机器人上电 |
| 自动使能 | `AutoEn()` | 自动使能 |
| 自动下电 | `AutoDn()` | 自动下电 |
| 断电 | `BlackOut()` | 断电 |
| 轴组使能 | `GrpEnable(nRbtID)` | 使能指定轴组 |
| 轴组禁能 | `GrpDisable(nRbtID)` | 禁能指定轴组 |
| 启动脚本 | `StartScript()` | 启动JBI脚本 |
| 暂停脚本 | `PauseScript()` | 暂停脚本 |
| 继续脚本 | `ContinueScript()` | 继续脚本 |
| 停止脚本 | `StopScript()` | 停止脚本 |
| 运行函数 | `RunFunc(name, params)` | 调用脚本函数 |
| 设置速度 | `SetOverride(id, speed)` | 设置速度倍率 |
| 急停 | `EmergencyStop()` | 紧急停止 |
| 复位 | `GrpReset()` | 复位 |
| 回零 | `MoveJToHome()` | 移动到零点 |

#### 15.4.4 一键巡检流程 (c_Work_Remote)

**类 `c_Work_Remote`** (`Work_Remote.cpp:449`)

巡检任务通过 `Axis_Position.json` 配置文件加载车型-轴位对应的脚本函数映射：

```json
{
  "HXD1": {
    "HXD1_01A_01左": "Func_Traction_Left",
    "HXD1_01A_01右": "Func_Traction_Right"
  }
}
```

**任务执行流程（Start_Cmd）：**

```
接收巡检指令 (JSON-RPC)
  → 参数校验 (Begin_Time, Track_ID, Car_Type, Car_Num, Head_Toward)
  → 加载车型巡检程序
  → wait_AutoEn_120()   // 左臂自动使能
  → wait_RunFunc_120("Func_1")  // 左臂执行
  → wait_AutoDn_120()   // 左臂自动下电
  → 退出任务
```

**等待机制：** 使用 `QEventLoop` 阻塞式等待，通过信号 `Work_Disconnected`/`Huayan_120_Moving` 等退出等待。

**MySQL 同步写入 (c_Sql_Remote)：**

- 连接信息从 `g_Communicate_DB` JSON 读取
- `Prec_Scan_Write_Done(imagePath)` — 写入采集记录队列
- `updateCollectTime()` — 逐条写入 `tbl_robot_detection_time` 表
- `updateActualReturnTime()` / `updateFastScanTime()` — 更新任务时间字段

#### 15.4.5 RGV 轨道巡检车 (Modbus TCP)

**类 `c_RGV_Client`** (`RGV_Client.cpp:407`)

- 协议：Qt5 QModbusTcpClient
- 数据类型：Coils / DiscreteInputs / InputRegisters / HoldingRegisters
- 配置参数：IP、Port、各类型起始地址和大小
- 最大缓存：每类 1000 个 uint16

### 15.5 子进程详细结构

每个子进程遵循统一的代码结构：

```
子进程/
├── CMakeLists.txt      (~250行，依赖 Qt5 + 对应 SDK)
├── main.cpp             (入口：解析 argv[1] 作为设备名)
├── Xxx.cpp/.h           (主 Widget，含 GUI + 设备驱动)
├── Xxx_Remote.cpp/.h    (stdin/stdout 通信处理)
├── Variable.cpp/.h      (共享：信号槽、工具函数)
├── Fr_Light.cpp/.h      (状态指示灯 Widget)
├── TCP_Client.cpp/.h    (TCP 客户端，仅某些子进程)
├── stuqss.css           (QSS 样式表)
├── Communicate_DB.json  (持久化配置)
└── Images/{icon.ico, Logo.jpg}
```

#### 15.5.1 Zivid_Camera (zivid_manage)

- SDK: Zivid SDK（C++ API）
- 功能：3D 点云采集、2D 图像保存、ZDF 文件输出
- 关键数据结构：`s_Zivid_Camera_DB` (Variable.h:314)
- 记录指标：settingTime, captureTime, save2DTime, save3DTime

#### 15.5.2 RealSense (realsense_manage)

- SDK: librealsense2
- 功能：D555 深度相机、PCL 点云
- 关键数据结构：`s_RealSense_DB` (Variable.h:301)

#### 15.5.3 RoboSense (robosense_manage)

- 协议：E1R 私有 UDP 协议

**数据包结构（Variable.h:148-208）：**

| 包类型 | 大小 | 同步字 | 数据单元 |
|--------|------|--------|----------|
| **MSOP** (测量包) | 1200 字节 | `0x55AA5AA5` | 96 个 s_DataBlock (各12字节) |
| **DIFOP** (设备信息包) | 256 字节 | 2个 uint32 同步字 | IP/端口/MAC/固件/温度 |

**类结构：**
- `c_E1R_MSOP` — MSOP 包接收与解析（96 个点/包）
- `c_E1R_DIFOP` — DIFOP 包接收与解析（设备配置信息）
- `c_Scan_Client` — TCP 客户端连接主进程

**点云可视化：** PCL + VTK 渲染 (RoboSense.h:70)
- `pcl::PointCloud<pcl::PointXYZI>::Ptr` 存储点云数据
- `vtkRenderer + vtkGenericOpenGLRenderWindow` 渲染
- `pcl::visualization::PCLVisualizer` 交互

**安全检测：** ROI 区域距离和点数阈值检测（障碍物判定）

#### 15.5.4 Hikvision (hikvision_manage)

- SDK: HCNetSDK（海康威视设备网络 SDK）
- 视频解码：PlayM4 SDK（YV12 格式）
- 关键数据结构：`s_Server_DB` (Variable.h:335)

#### 15.5.5 Transmission_Server (transmission_server)

- 功能：双端口 TCP 文件传输服务
- 特性：MD5 完整性校验、断点续传
- 关键数据结构：`s_Transmission_DB` (Variable.h:346)
  - `buffer` / `Tran_buffer` / `Retran_buffer` — 传输队列

#### 15.5.6 HIPNUC_CH10X (hipnuc_ch10x_manage)

- 硬件：CH10X IMU 惯性导航传感器
- 接口：UART 串口 (`/dev/ttyUSB0`, 115200 bps, 200Hz)
- 算法：卡尔曼滤波 (KF) 里程计解算

**关键数据结构：**
- `s_HIPNUC_CH10X_DB` (Variable.h:546): 原始传感器数据（加速度/陀螺仪/磁力/欧拉角/四元数）
- `s_IMU_Odom` (Variable.h:566): 里程计输出 (x, y, vx, vy, yaw, 静止标志)

### 15.6 全局数据结构（单例模式）

**`c_Variable`** (`Variable.h:611`) — 线程安全的单例全局状态管理器：

| 变量 | 类型 | 用途 |
|------|------|------|
| `g_Communicate_DB` | `QJsonObject` | 全局配置（IP/端口/参数） |
| `g_Worry_List` | `QJsonArray` | 警告/日志列表 |
| `g_Work_List` | `QJsonArray` | 工作日志列表 |
| `g_Work` | `s_Work_DB` | 当前巡检任务信息 |
| `g_Huayan_120` / `g_Huayan_121` | `s_Huayan_DB` | 双机械臂连接状态 |
| `g_Prec_Scan_120` / `g_Prec_Scan_121` | `s_Scan_DB` | 精扫转发状态 |
| `g_Datasheet_120` / `g_Datasheet_121` | `s_Datasheet_Data` | 双机械臂实时数据（6关节+TCP+IO+错误码） |
| `g_RGV` | `s_RGV_DB` | RGV Modbus 寄存器缓存 |
| `g_Local_Remote` | `s_Robot_DB` | 本地客户端连接计数 |
| `g_HIPNUC_CH10X` | `s_HIPNUC_CH10X_DB` | IMU 传感器数据 |
| `g_IMU_Odom` | `s_IMU_Odom` | IMU 里程计数据 |

### 15.7 关键数据协议

#### 15.7.1 E1R 激光雷达协议（自定义二进制结构体）

```cpp
// MSOP 包 (1200字节 = 32+96*12+16)
s_MSOPPacket {
    s_MSOPHeader header;          // 32字节
    s_DataBlock blocks[96];       // 96个数据块，各12字节
    s_MSOPTrailer trailer;        // 16字节 (dataLength=0x04B0, dataId=0x00000E5C)
}

// s_DataBlock (12字节) - 每个数据点
{
    uint16_t timeOffset;          // 时间偏移 (μs)
    uint16_t radius;              // 距离 (mm)
    int16_t  dirVectorX/Y/Z;      // 单位方向向量 (×1000)
    uint8_t  intensity;           // 反射强度
    uint8_t  pointAttr;           // 点属性 (bit0: 回波标志)
}

// DIFOP 包 (256字节) - 设备信息
s_DIFOPPacket {
    s_IPv4Address localIp/msopRemoteIp/difopRemoteIp;
    uint16_t msopLocalPort/msopRemotePort;
    uint16_t difopLocalPort/difopRemotePort;
    s_MACAddress macAddress;
    s_FirmwareVersion swVersion;
    uint8_t frequencySetting/returnMode/timeSyncMode;
}
```

#### 15.7.2 华研机械臂 DataSheet 协议

基于 V1.0.19.1 协议的 `s_Datasheet_Data` 结构体 (Variable.h:372-477)：

| 模块 | 字段 | 说明 |
|------|------|------|
| **PosAndVel** | Actual_Position[12], Actual_PCS_TCP[6], Actual_PCS_Base[6] | 关节位置(°), TCP坐标(mm/°), Base坐标 |
| | Actual_Joint_Current[6], Actual_Joint_Velocity[6], Actual_Joint_Acceleration[6] | 关节电流/速度/加速度 |
| | Actual_Override | 速度率(0~1) |
| **EndIO** | EndDI[4], EndDO[3], EndButton[4] | 末端数字IO/按钮 |
| **ElectricBoxIO** | BoxCI[8], BoxCO[8], BoxDI[8], BoxDO[8] | 电箱数字IO |
| **StateAndError** | robotState, robotEnabled, robotMoving, InPos, Error_Code | 状态机/错误码 |
| **FTData** | FTControlState, FTData[6], FTSrcData[6] | 力传感器数据 |
| **HardLoad** | EtherCAT_TotalFrame/FramesPerSecond/TotalLostFrame | EtherCAT统计 |
| | joint_temperature[6], joint_Voltage[6] | 关节温度/电压 |
| **SafePlane** | SafePlane_Name/Distance/Trendency | 安全平面监测 |
| **ConstraintArea** | ConstraintArea_Name/Distance/Trendency | 安全空间监测 |
| **Script** | Script_errorCode, Script_cmdid[6] | 脚本执行状态 |
| **Modbus** | Modbus_connectStatus, Modbus_errorCode | Modbus从站状态 |

#### 15.7.3 精扫采集协议（Scan_Server）

```
机械臂 → 主进程 (TCP):
  START!                              // 开始一段采集
  PHOTO&R-ZDZZ-05-08!                // 拍照指令: 部件位号-一级-二级-巡检点
  FINISH!                             // 当前段采集完成

主进程 → 机械臂 (TCP):
  <START&SUCCESSED&0!>               // 采集初始化成功
  <PHOTO&T1&SUCCESSED&0!>            // 拍照完成 (相机编号)
```

**照片命名规范：**
```
N-T1-20240423-HXD10001-AB-A-01-01-R-ZDZZ-05-08-2.jpg
│  │  │         │          │ │ │  │  │ │  │    │ │  │
│  │  日期     车型车号    进车 车转 轴 相 部 一级 二级 巡检点
│  │                     方向 厢向 号 机 位  部件 部件
│  相机编号                       架  编 号
任务编号前缀                       号  号
```

### 15.8 地图定位组件 (Map_Widget)

**类：** `Map_Widget` (`Map_Widget.cpp:164`)

基于 Qt `QGraphicsView` 实现的 2D 实时定位地图：

- **坐标系：** 惯导 (X东, Y北) → Scene (X右, Y下)，即 Y轴取反
- **动态扩展：** sceneRect 自动扩展 10m 余量
- **轨迹绘制：** 使用独立 `QGraphicsLineItem` 逐段绘制（最多保留 10000 段）
- **网格背景：** 自适应多级刻度网格 (5m/0.5m)，缩放时自动切换
- **机器人图标：** 红色多边形，按 yaw 角旋转
- **交互：** 鼠标滚轮缩放、右键拖拽平移

### 15.9 状态指示灯 (Fr_Light)

**类：** `c_Fr_Light` (`Fr_Light.h`)

| 状态 | 颜色 | 信号 | 含义 |
|------|------|------|------|
| Working | 绿色 | `Working_State()` | 设备工作中 |
| Default | 红色 | `Default_State()` | 设备未连接 |
| Error | 黄色 | `Error_State()` | 设备错误 |
| Null | 灰色 | `Null_State()` | 设备未初始化 |

通过信号自动联动按钮的 disabled/enabled 状态。

### 15.10 工具基类 (c_Object)

**类：** `c_Object` (`Variable.h:576`)

提供公共工具方法：

| 方法 | 功能 |
|------|------|
| `BitToFloat()` / `FloatToBit()` | Modbus 16位高低字 ↔ 浮点数转换 |
| `IntToBit()` / `BitToInt()` | 32位整数 ↔ 16位高低字转换 |
| `QtPing(ip)` | ICMP Ping 检测网络可达性 |
| `TCP_Status(int)` | TCP 状态码 → 可读字符串 |
| `Modbus_Status(int)` | Modbus 状态 → 可读字符串 |
| `Hikvision_Status(DWORD)` | 海康 SDK 错误码 → 可读字符串 |
| `Huayan_Status(int)` | 华研机械臂状态码 → 可读字符串 |
| `CanOpen(int)` | CANopen 状态 → 可读字符串 |

### 15.11 依赖库清单

| 库 | 用途 | 模块 |
|----|------|------|
| Qt5 Core/Gui/Widgets/Network | GUI + TCP + 线程 | 全部 |
| Qt5 Charts | 数据图表 | 主进程 |
| Qt5 SerialPort | 串口通信 | IMU |
| Qt5 WebSockets | WebSocket 通信 | 主进程 |
| Qt5 OpenGL | 3D 渲染 | 主进程 |
| Qt5 Concurrent | 并发编程 | 全部 |
| QModbus | Modbus TCP | RGV |
| PCL 1.14 | 点云处理（滤波/分割/聚类） | 主进程, RoboSense, RealSense |
| VTK 9.1 | 点云可视化渲染 | 主进程, RoboSense, RealSense |
| OpenCV | 图像处理 | 全部 |
| Zivid SDK | 3D 相机控制 | Zivid_Camera |
| librealsense2 | RealSense D555 | RealSense |
| HCNetSDK + PlayM4 | 海康相机控制与解码 | Hikvision |
| MySQL Client | 数据库写入 | 主进程 (c_Sql_Remote) |
| Eigen | 线性代数（KF 滤波） | HIPNUC_CH10X |
| Boost | PCL 依赖 | 全部 |
| CUDA 13.0 | GPU 加速 | 全部 |

### 15.12 配置文件

| 文件 | 格式 | 用途 |
|------|------|------|
| `Collector_Manage.json` | JSON | 全局设备配置（IP/端口/参数） |
| `Communicate_DB.json` | JSON | 各子进程本地配置（预留） |
| `Axis_Position.json` | JSON | 巡检任务脚本函数映射表 |
| `stuqss.css` | CSS/QSS | GUI 皮肤样式表 |
| `Debug_DB/` 目录 | 文本日志 | 按时间戳命名的调试日志 |

### 15.13 部署与构建

```bash
# 构建脚本 (build.sh)
cd /home/nvidia/Collector_Manage
mkdir -p build && cd build
cmake .. \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_CUDA_ARCHITECTURES=120 \
    -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
make -j$(nproc)

# 可执行文件输出
build/bin/collector_manage       # 主进程
build/bin/zivid_manage           # Zivid 子进程
build/bin/realsense_manage       # RealSense 子进程
build/bin/robosense_manage       # E1R LiDAR 子进程
build/bin/hikvision_manage       # Hikvision 子进程
build/bin/transmission_server    # 传输服务子进程
build/bin/hipnuc_ch10x_manage    # IMU 子进程
```

### 15.14 子进程核心实现详解

#### 15.14.1 HIPNUC_CH10X IMU 里程计（256行）

**类：** `c_HIPNUC_CH10X_Remote` (`HIPNUC_CH10X_Remote.cpp:256`)

**完整数据管道：** 串口(115200bps) → CRC16校验 → 帧同步(5A A5, tag=0x91) → parseFrame(76字节) → 里程计解算 → 10Hz发布(stdout)

**里程计核心算法（10步）：**
1. Body系零偏在线补偿（静止时采100帧取均值，G→m/s²转换）
2. 四元数旋转 Body(RFU) → ENU（归一化+旋转矩阵）
3. 一阶低通滤波 (α=0.4, 截止频率~5.7Hz)
4. 加速度死区 (2mg) 和速度死区 (1cm/s)
5. **防抖晃动抑制**（连续20帧矛盾条件：速度小但加速度大 → 强制置零）
6. 外部静止硬重置（is_static时速度清零）
7. 运动学积分（vel += acc×dt, pos += vel×dt）
8. 四元数→Yaw提取
9. **ENU→地图系转换**（map_y=-y, map_yaw=π/2-yaw_enu）
10. 降采样发布（10Hz, 格式 `IMU_ODOM&x&y&vx&vy&yaw&timestamp&static`）

**高性能模式AT指令：** `AT+EOUT=0` → `AT+MODE=0` → `AT+ODR=100` → `AT+RST`

#### 15.14.2 Zivid 3D 相机采集管线（612行）

**线程架构：** GUI主线程 + 相机采集线程(Zivid SDK) + TCP文件传输线程

**采集完成后的管道：** JPG图像(cv::imread) + .xyz点云(int16×3, BigEndian, scale=0.1) + .png深度图 → PCL PointXYZRGB(BGR映射) → VTK渲染 → 文件入传输队列 → stdout "CaptureCompleted&"

**传输队列：** Tran_buffer(优先发送) + Retran_buffer(失败重传)，支持 .jpg/.png/.xyz/.data，自动过滤 >200MB 文件

#### 15.14.3 RoboSense E1R 雷达界面（539行）

**线程架构：** GUI主线程 + MSOP线程(UDP 1200字节/包→96点→PointXYZI) + DIFOP线程(UDP 256字节/包→设备信息)

**DIFOP显示面板：** 固件版本/SW序列号/MAC地址/帧率/回波模式(7种)/时间同步状态/PHY模式(3种)/芯片温度(T=raw-80)/时间戳(10字节大端序秒+微秒)

**ROI障碍物检测：** 区域距离 < 安全距离 且 点数量充足 → 绿灯(安全)；否则红灯(障碍)

#### 15.14.4 Transmission_Server 文件传输（414行）

**JSON协议状态机：** `{cmd:"file_info", filePath, fileSize, hash:MD5}` → START确认 → 二进制数据逐块写入 → MD5校验 → FINISH/ERROR响应

**安全特性：** 文件类型白名单(.jpg/.png/.xyz/.data)、MD5完整性校验、60秒超时断开、"PreScan_raw/"路径截断自动创建目录

#### 15.14.5 华研机械臂监控器（Huayan_Monitor, 599行）

解析DataSheet的 **13个JSON模块**：PosAndVel(关节/TCP/电流/速度/加速度/Override)、EndIO、ElectricBoxIO、ElectricBoxAnalogIO、StateAndError(状态机/使能/运动/到位/错误码/抱闸)、FTData(力传感器)、HardLoad(EtherCAT/温度/电压)、Modbus、RobotAuthorization、SafePlane(安全平面)、ConstraintArea(安全空间)、Script(脚本状态)、MsgTitle(时间戳)

**安全JSON解析 (safeParseDouble)：** 兼容String和Number类型，修复协议下发字符串数组导致toDouble()静默返回0的问题

#### 15.14.6 其他子进程要点

| 子进程 | 核心实现 |
|--------|----------|
| **Hikvision** (261行) | `NET_DVR_Init()` → `Login_V40` → `RealPlay_V40` + `RealDataCallBack_V30` → `PlayM4` 解码YV12→RGB→QImage显示 |
| **RealSense** (281行) | `rs2::pipeline` + `rs2::align` + `rs2::pointcloud` → D555 1280×720@30fps → PCL PointXYZRGB + VTK渲染 |

---

## 十六、开发调试经验集（Qwen历史对话提炼）

> **来源：** `C:\Users\Administrator\Desktop\机车巡检机器人\Qwen历史对话信息\` 目录下 81 个聊天记录文件
> **涵盖主题：** E1R协议调试、机械臂DataSheet修复、网络故障排查、Zivid优化、IMU校准、Jetson环境部署

### 16.1 E1R 雷达 MSOP/DIFOP 协议关键修正

#### 16.1.1 距离过滤条件 Bug（已修复）

**原始代码问题** (`E1R_MSOP.cpp`):
```cpp
if (100.f > x || x > 30000.0f) { continue; }  // 有风险
```

**问题分析：**
- `distance = radius * 5.0f` 单位是**毫米**（手册4.3.1.2：Radius分辨率5mm）
- E1R盲区为0.1m(100mm)，但 X负值点（后方）会被错误保留
- 条件 `100.f > x` 写法不标准

**修正方案：**
```cpp
if (x < 50.0f || x > 30000.0f) { continue; }  // 0.05m~30m有效范围
```

#### 16.1.2 强制结构体对齐（跨平台兼容）

E1R的MSOP协议是**紧凑二进制协议（无填充）**，必须强制1字节对齐：

```cpp
#if defined(_MSC_VER)
    #define PACKED_STRUCT __pragma(pack(push, 1)) struct
    #define PACKED_END __pragma(pack(pop))
#elif defined(__GNUC__) || defined(__clang__)
    // Jetson Thor ARM64需特殊处理
    #if defined(__aarch64__) || defined(__arm64__)
        #define PACKED_STRUCT struct __attribute__((packed, aligned(1)))
    #else
        #define PACKED_STRUCT struct __attribute__((packed))
    #endif
    #define PACKED_END
#endif
```

**编译期验证（必须添加）:**
```cpp
static_assert(sizeof(s_Timestamp) == 10, "s_Timestamp size mismatch!");
static_assert(sizeof(s_MSOPHeader) == 32, "s_MSOPHeader size mismatch!");
static_assert(sizeof(s_DataBlock) == 12, "s_DataBlock size mismatch!");
static_assert(sizeof(s_MSOPTrailer) == 16, "s_MSOPTrailer size mismatch!");
static_assert(sizeof(s_MSOPPacket) == 1200, "s_MSOPPacket size mismatch!");
static_assert(sizeof(s_DIFOPPacket) == 256, "s_DIFOPPacket size mismatch!");
```

#### 16.1.3 推荐按协议偏移解析（最安全）

即使强制对齐，**生产环境强烈推荐从 raw 数组按偏移解析**，避免编译器差异、优化等级等潜在风险：

```cpp
// 推荐：E1RProtocol 命名空间辅助函数
namespace E1RProtocol {
    inline uint16_t be16(const uint8_t* raw, size_t offset) {
        return (static_cast<uint16_t>(raw[offset]) << 8) | raw[offset + 1];
    }
    inline float getRadiusMM(const uint8_t* raw, size_t block_idx) {
        size_t offset = 32 + block_idx * 12 + 2; // header(32) + radius偏移(2)
        return static_cast<float>(be16(raw, offset)) * 5.0f; // 5mm分辨率
    }
}
```

#### 16.1.4 CRC32 校验缺失

手册4.3.1.3节明确MSOP帧尾1196-1199字节包含CRC32，但当前代码未验证：
```cpp
// 需实现CRC32校验（覆盖前1196字节）
uint32_t received_crc = be32(raw, 1196);
uint32_t calculated_crc = crc32(raw, 1196);
if (received_crc != calculated_crc) {
    emit Status("CRC校验失败，丢弃损坏数据包");
    return;
}
```

#### 16.1.5 字节序处理

**E1R所有多字节字段均为大端序（Big-Endian）**。在小端CPU上直接访问结构体成员会发生字节序错误：
- 正确：`qFromBigEndian<uint32_t>(data)` (Qt) 或 `__builtin_bswap32()` (GCC)
- 错误：直接把 `uint16_t msopLocalPort` 当本地字节序使用
- 单字节字段（`lidarTmp`, `frequencySetting`, `returnMode`）**无字节序问题**

#### 16.1.6 DIFOP 解析修正

**SN序列号显示：** ASCII编码(6字节)，直接 `QString::fromLatin1()` 后无需追加16进制
**帧率映射：** `0x0A` = 10Hz（默认），`0x05` = 5Hz，`0x14` = 20Hz
**MAC显示：** 分隔符使用 `:` 而非 `-`
**同步头验证：** `0x55AA55AA` + `0x5A05AA5A`（双32位同步字）

### 16.2 华研/大族机械臂 DataSheet 解析关键修复

#### 16.2.1 关节位置/TCP位置始终为零（已修复）

**根本原因：** 机械臂 DataSheet 协议下发的位置数据是 **JSON 字符串数组**格式：
```json
"Actual_Position": ["0.000", "0.000", "81.099", ...]  // 字符串，非数字！
```

`QJsonValue::toDouble()` 仅对 JSON Number 类型有效，对 String 类型会静默返回 `0.0`。

**修复方案 — 兼容解析 Lambda：**
```cpp
auto safeParseDouble = [](const QJsonValue& val) -> double {
    if (val.isDouble()) return val.toDouble();   // 兼容 Number 类型
    if (val.isString()) return val.toString().toDouble();  // 修复：String→double
    return 0.0; // 异常类型兜底
};
```

**影响范围：** `Actual_Position`, `Actual_PCS_TCP`, `Actual_PCS_Base`, `Actual_Joint_Current`, `Actual_Joint_Velocity`, `Actual_Joint_Acceleration`, `FTData`, `joint_temperature` 等所有数组字段。

#### 16.2.2 速度控制优化

**RunFunc 脚本运行缓慢的解决方案：**

```cpp
// 协议 4.5.1 节：SetOverride,nRbtID,dOverride,;
void c_Huayan_Remote::SetOverride(int nRbtID, double dOverride) {
    dOverride = std::max(0.01, std::min(1.0, dOverride)); // 限制 1%~100%
    QStringList params;
    params << QString::number(nRbtID) << QString::number(dOverride, 'f', 2);
    SendCommand(Build_Command("SetOverride", params));
}
// 在 RunFunc 前调用：m_Huayan_Remote_120->SetOverride(0, 1.0);
```

**速度慢的排查点：**
| 原因 | 排查方法 | 解决 |
|------|----------|------|
| Reduce Mode | 检查 `IsReduceMode == 1` | 缩减模式强制限速30%，需切换正常模式 |
| 脚本内速度写死 | 检查 `MoveJ/MoveL` 的 `Velocity` 参数 | `Override` 等比缩放基准值 |
| 安全碰撞等级过高 | 检查 `SetCollideLevel` | 等级4~5限制最大速度 |

### 16.3 Jetson 多网卡网络故障排查

#### 16.3.1 网络拓扑（Jetson AGX Thor 实际配置）

基于 `ip addr` 输出确认的网卡配置：

| 网卡 | IP | 用途 |
|------|-----|------|
| `enP3p3s0` | `192.168.1.35` | - |
| `enP3p4s0` | `192.168.30.178/23` | 默认网关 (`192.168.31.1`) |
| `enP3p5s0` | `192.168.254.35/24` | **E1R #1** (192.168.254.30) |
| `enP3p6s0` | `192.168.253.35/24` | **E1R #2** (192.168.253.30) |
| `mgbe0_0` | `192.168.3.35` | - |
| `mgbe1_0` | `192.168.2.100` | - |
| `can0~can3` | - | CAN总线（Down状态） |
| `l4tbr0` | `192.168.55.1` | L4T USB桥（Down状态） |
| `docker0` | `172.17.0.1` | Docker网桥（Down状态） |

**MAC地址：** `enP3p3s0~enP3p6s0` 使用连续MAC (`00:a0:c9:00:00:82~85`)，属同一块4口PCIe扩展网卡。

#### 16.3.2 UDP 断开重连后收不到包 — 根因与解决

**诊断流程：**

1. **ARP 验证：** `arp -a | grep 192.168.253.30`
   - `<incomplete>` → 物理链路不通
   - `at XX:XX:XX:XX:XX:XX` → 链路正常

2. **tcpdump 一锤定音：**
   ```bash
   sudo tcpdump -i enP3p6s0 -n -e host 192.168.253.30
   ```
   - 无任何包 → EIR设备端固件Bug（客户端断开后发送线程卡死）
   - 有包但目的端口是旧端口 → 本地代码端口随机分配问题

3. **修复方案 — 固定本地UDP端口：**
   ```python
   sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
   sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)  # 端口复用
   sock.bind(('192.168.253.35', 8888))  # 固定端口，不让OS随机分配
   ```

4. **架构级建议：** 永远不要主动 `close()` UDP Socket，改为发送心跳包保持连接活跃

### 16.4 Zivid 3D 相机 SDK 使用模式

```cpp
// 初始化
Zivid::Application zivid;

// 连接（多相机时按序列号）
auto cameras = zivid.cameras();
auto camera = zivid.connectCamera(
    Zivid::CameraInfo::SerialNumber{"2020C0DE"}
);

// 配置（预设或手动）
camera << Zivid::Settings::Acquisitions{
    Zivid::Settings::Acquisition{
        Zivid::Settings::Acquisition::ExposureTime{8000},
        Zivid::Settings::Acquisition::Gain{1.0}
    }
};

// 捕获3D+2D
auto frame = camera.capture(settings);
auto pointCloud = frame.pointCloud();
auto image2D = frame.image2D();

// 保存
frame.save("/path/to/file.zdf");
```

### 16.5 CH10X IMU 调试经验

#### CRC 校验失败常见原因：
1. **波特率不匹配：** CH104-USB 默认115200，如需400Hz需AT指令修改
2. **串口数据粘包/断帧：** 需实现帧同步（同步字0x5AA5 + 长度校验）
3. **CRC计算范围错误：** 需确认CRC覆盖的数据范围（不含帧头同步字）

### 16.6 Jetson 环境部署关键配置

#### 16.6.1 防火墙配置
```bash
# Thor 机需关闭防火墙（工业内网环境）
sudo ufw disable
# 或开放特定端口
sudo ufw allow 10003/tcp    # 机械臂控制
sudo ufw allow 10005/tcp    # 机械臂监控
```

#### 16.6.2 MySQL 安装 (Ubuntu 24.04 ARM64)
```bash
sudo apt install mysql-server mysql-client libmysqlclient-dev
sudo mysql_secure_installation
# 创建数据库和表
mysql -u root -p
CREATE DATABASE robot_inspection;
CREATE TABLE tbl_robot_inspection_tasks (...);
CREATE TABLE tbl_robot_detection_time (...);
```

#### 16.6.3 跨设备部署
```
开发机(Windows) → scp/rsync → Jetson Thor(/home/nvidia/Collector_Manage/)
├── 源码 → cmake → make → build/bin/
├── 配置文件 → /home/nvidia/Collector_Manage/
└── 调试日志 → /home/nvidia/Collector_Manage/Debug_DB/
```

### 16.7 TCP 通信调试经验

#### 16.7.1 TCP粘包/半包处理
大族机械臂 `LTBR`+长度头协议中，`readyRead` 未显式实现帧边界重组。建议：
```cpp
void c_Huayan_Monitor::Read_Byte_Done(QByteArray data) {
    m_buffer.append(data);
    while (m_buffer.size() >= 4) {
        // 解析 LTBR 包头 + 长度字段
        if (m_buffer.left(4) != "LTBR") { m_buffer.clear(); return; }
        int jsonLen = /* 从 m_buffer[4..7] 解析大端序长度 */;
        if (m_buffer.size() < 8 + jsonLen) return; // 等待更多数据
        QByteArray jsonData = m_buffer.mid(8, jsonLen);
        m_buffer.remove(0, 8 + jsonLen);
        parseJSON(jsonData);
    }
}
```

#### 16.7.2 TCP Server 多客户端管理
`c_Robot_Server` 支持多客户端广播发送，`c_Scan_Server` 为严格一对一模式（新连接会 abort 旧连接）。

### 16.8 机械臂示教器 UI 设计参考

基于《大族机械臂Elfin软件操作说明书V6.4》和《HuayanRobotV6协议》设计的完整示教器界面包含：

| 功能区 | 关键组件 | 关联协议 |
|--------|----------|----------|
| 顶部状态栏 | FSM/使能/运动/错误码 | DataSheet StateAndError |
| 运行与监控 | 脚本启停、函数调用、日志 | StartScript, RunFunc |
| 点动示教 | 空间/关节点动、步进切换 | ShortJogL, LongJogJ |
| 脚本编程 | 指令树、参数编辑、WayPoint | MoveJ/MoveL/WayPoint |
| 轨迹管理 | 采集启停、PathJ/PathL | InitPath, PushPathPoints |
| 系统配置 | TCP设置、负载辨识、安全 | SetPayload, SetCollideLevel |
| IO监测 | 电箱/末端IO状态 | ReadBoxDI, SetBoxDO |
| 底部控制 | 急停/复位/使能/回原点/速度 | EmergencyStop, GrpReset, SetOverride |

### 16.9 源码架构设计亮点

根据Qwen对源码的分析：

1. **监控与控制解耦** — Monitor 负责高频状态订阅与解析，Remote 专注指令下发与响应
2. **线程安全通信** — 所有网络IO下沉至独立 QThread，通过 `Qt::QueuedConnection` 跨线程
3. **协议容错处理** — `ParseResponse` 使用正则匹配 `OK/Fail`，支持变长参数解析
4. **UI嵌入安全策略** — 二次指针校验、布局递归清理、延迟 `updateGeometry`
5. **跨线程安全发送** — `QMetaObject::invokeMethod(..., Qt::QueuedConnection, Q_ARG(...))`
6. **可优化项** — TCP粘包处理、同步等待机制 (`QEventLoop`)、配置热更新 (内存缓存+定时持久化)、日志分级 (qDebug/qWarning/qCritical)

---

---

## 十七、全国机务段项目资料补充

> **来源：** `C:\Users\Administrator\Desktop\机车巡检机器人\资料\怀化项目\`、`南昌项目\`、`南宁\`
> **注意：** 怀化/南昌/杭州/乌鲁木齐为北铁所+普华灵动方案，电控架构与南宁（国智中心）方案本质不同

### 17.1 怀化项目（北铁所 + 普华灵动方案）

#### 17.1.1 精扫相机选型

进行了多厂商3D相机对比评估：

| 厂商 | 相机型号 | 评估状态 |
|------|----------|----------|
| **Zivid** | Zivid 2+ M60 | 最终选定（2台，序列号 24140107 / 2414AF76） |
| 如本科技 | - | 对比评估 |
| 凌云光 | - | 对比评估 |
| 立创致恒 | - | 对比评估 |
| 汉振 | - | 对比评估 |

#### 17.1.2 采集软件版本演进（ZividQtExample）

4个迭代版本，从简单到复杂：

| 版本 | 目录 | 功能演进 |
|------|------|----------|
| **V1** | `ZividQtExample(1)/` | 单相机、UDP拍照完成通知、试用期至2024-09-30 |
| **V2** | `1022/` | 单相机、新增globals.cpp/httpthread.cpp、独立上传日志 |
| **V3** | `ttt/ZividQtExample/` | **双相机T1/T2**、TCP拍照完成通知、新增GetStatus指令、试用期至2024-11-30 |
| **V4** | `fggg/` (12精扫相机/7.采集软件/) | **最完善版本**：双相机、双TCP端口(21000/22000)、2D/3D并行保存线程、HTTP上传线程、自动清理旧数据、YAML独立配置文件、BigEndian二进制.xyz/.data格式、试用期至2024-12-30 |

#### 17.1.3 V4 核心通信协议

**TCP配置 (`tcp.txt`)**:
```
192.168.10.35    # 采集机IP
21000            # Port1 (调度指令)
22000            # Port2 (备用)
```

**调度指令协议 (`&`分隔, `<>`不包裹)**:
```
START&J11&20240423&1143&CR400AF2200&202404231143CR400AF2200
  → 初始化任务（股道号&日期&时分&车型车号&任务编号）

CHANGE&GAIN&10&T1
  → 调整增益（GAIN&值&相机编号）

PHOTO&01A&T1-20211106-CRH2A2411-01-A-01-01-ZDZZ-05-08
  → 拍照指令（转向架号&相机-日期-车型车号-车厢-转向架-轴-位号-一级部件-二级部件-巡检点）
```

**HTTP上传配置 (`url.txt`)**: `http://192.168.0.201:8080/upload`

**自动清理配置 (`autodelete.txt`)**:
```
../data                            # 数据目录
15                                  # 保留天数
/media/bjtds/RawImageDisk/PreScan_raw  # 原始数据备份路径
-1                                  # 备份保留天数(-1=永久)
```

#### 17.1.4 V4 核心采集流程 (mainwindow.cpp 核心逻辑)

```cpp
// 1. 双相机管理
cameras.resize(2);
cameraConnected.resize(2);
CameraBusy.resize(2, 0);
gains.resize(2, 1);

// 2. TCP配置从文件读取 (tcp.txt: IP, Port1, Port2)
// 3. 启动时自动启动TCP服务器
// 4. 2D/3D参数滑块联动 (exposureTime, aperture, brightness, gain)
// 5. 深度图转short保存 (float*10 → short, NaN→-32768)
float2short(data) {
    if(isnan(data)) return -32768;
    else return static_cast<short>(data*10);
}

// 6. 试用期检查
checkTrialExpiration() {  // 2024-12-30 截止 }
```

#### 17.1.5 安全连锁接入

| 文件 | 内容 |
|------|------|
| `安全连锁方案.docx` | 安全联动方案 |
| `安全连锁接入.pptx` | 接入方案演示 |
| PNOZ安全继电器手册 | 硬件安全模块 |

#### 17.1.6 网络拓扑

```
采集机(192.168.10.35)
  ├── Zivid T1 (USB 3.0)
  ├── Zivid T2 (USB 3.0)
  ├── 交换机 → 调度系统(192.168.0.201:8080)
  └── 安全继电器(PNOZ)
```

#### 17.1.7 Python 测试脚本（模拟调度系统）

`12精扫相机/测试软件/` 目录下 9 个 Socket 测试客户端：

| 脚本 | 功能 |
|------|------|
| `T1.py` / `T2.py` | 分别测试T1/T2相机，TCP连接发送拍照指令 |
| `T1_0624_1048.py` ~ `T1_1202_1707.py` | 多版本迭代的T1测试脚本 |
| `T2_0624_1048.py` ~ `T2_1129.py` | 多版本迭代的T2测试脚本 |
| `test.py` | 基础TCP测试客户端 |

**核心测试逻辑**（典型脚本）：
```python
# 连接采集软件
sock.connect(('192.168.10.35', 21000))
# 发送START指令
sock.send(b'START&J11&20240423&1143&CR400AF2200&202404231143CR400AF2200')
# 发送PHOTO指令  
sock.send(b'PHOTO&01A&T1-20240423-CR400AF2200-01-A-01-01-R-ZDZZ-05-08')
# 接收响应
data = sock.recv(1024)
```

### 17.2 补充技术参考

#### 17.2.1 海康威视 SDK Python 绑定（ctypes）

完整 `HCNetSDK.py` (617行) 提供所有SDK函数和结构体的Python ctypes绑定：

**设备管理：** `NET_DVR_Init()`, `NET_DVR_Login_V40()`, `NET_DVR_Logout()`
**实时预览：** `NET_DVR_RealPlay_V40()` + `REALDATACALLBACK` 码流回调
**报警管理：** `NET_DVR_SetupAlarmChan_V41()` + `MSGCallBack_V31`
**透传配置：** `NET_DVR_STDXMLConfig()` (GET/PUT ISAPI)
**解码播放：** PlayCtrl.dll 函数 (`PlayM4_GetPort`, `PlayM4_OpenStream`, `PlayM4_InputData`)

4 个完整示例 Demo：

| Demo | 功能 | 关键API |
|------|------|---------|
| `test_main.py` (230行) | 实时预览+解码+抓图+PTZ | `NET_DVR_RealPlay_V40` → `PlayM4_OpenStream` → YUV→JPEG |
| `AlarmTest.py` (257行) | 主动布防监听 | `NET_DVR_SetupAlarmChan_V41`, 处理移动侦测/门禁/身份证/ISAPI报警 |
| `ListenTest.py` (220行) | 被动报警监听 | `NET_DVR_StartListen_V30` |
| `DemoTest.py` (184行) | ISAPI协议透传 | `NET_DVR_STDXMLConfig` GET/PUT |

#### 17.2.2 rslidar_sdk 核心配置 (config.yaml)

```yaml
common:
  msg_source: 1           # 1=在线雷达, 2=ROS包, 3=PCAP文件
  send_packet_ros: false  # 是否转发原始包到ROS
  send_point_cloud_ros: true # 是否发布点云到ROS

lidar:
  - driver:
      lidar_type: RSM1    # 雷达型号
      frame_id: rslidar
      msop_port: 6699     # MSOP数据端口
      difop_port: 7788    # DIFOP数据端口
      imu_port: 6688      # IMU数据端口 (v1.5.17+)
      wait_for_difop: true
    ros:
      ros_frame_id: rslidar
      ros_send_point_cloud_topic: /rslidar_points
      ros_send_packet_topic: /rslidar_packets
      ros_send_imu_data_topic: /rslidar_imu_data
```

#### 17.2.3 rs_driver 错误码速查

| 错误码 | 含义 |
|--------|------|
| `ERRCODE_SUCCESS (0)` | 成功 |
| `ERRCODE_MSOPTIMEOUT (0x01)` | MSOP 超时 |
| `ERRCODE_NODIFOPRECV (0x02)` | 未收到 DIFOP |
| `ERRCODE_WRONGMSOPLEN (0x04)` | MSOP 包长度错误（应1200字节） |
| `ERRCODE_WRONGMSOPID (0x08)` | MSOP 包 ID 错误 |
| `ERRCODE_PKTBUFOVERFLOW (0x10)` | 接收缓冲区溢出 |
| `ERRCODE_CLOUDOVERFLOW (0x20)` | 点云帧点数超限 |
| `ERRCODE_WRONGCRC32 (0x40)` | CRC32 校验失败 |
| `ERRCODE_WRONGIMULEN (0x80)` | IMU 包长度错误 |
| `ERRCODE_ZEROPOINTS (0x100)` | 点云帧内无有效点 |

#### 17.2.4 rs_driver 线程模型

```
┌──────────────┐    pkt_queue    ┌──────────────┐   stuffed_queue   ┌──────────────┐
│  recv_thread │ ──────────────→ │ handle_thread│ ────────────────→ │ process_thread│
│  (Input层)   │    free_queue   │ (Decoder层)  │   free_queue      │  (用户处理)   │
└──────────────┘ ←────────────── │              │ ←──────────────── │              │
    UDP/Socket                     帧组装+解码                      点云处理/发布
```

#### 17.2.5 LidarAssistant 版本演进

| 版本 | 日期 | 关键功能 |
|------|------|----------|
| V1.1.1.0 | 2024-04-13 | 初版，支持 E1 项目 |
| V1.2.0.0 | 2024-07-02 | DoIP/CAN 通信，固件升级 |
| V1.2.2.0 | 2024-09-28 | 寄存器读写，PCAP 回放 |
| V1.3.0.0 | 2024-11-01 | OpenGL 点云可视化 |
| V1.4.0.0 | 2024-12-19 | 支持多项目(0270/0800/0278/0620/0630) |
| V1.4.1.0 | 2025-01-23 | E1 增强，项目切换优化 |

### 17.3 怀化项目采集软件服务器端

`15.采集机软件/server.py` (39行) 是一个简易 HTTP 文件上传接收服务：
- 监听指定端口接收 POST 文件上传
- 保存上传文件到本地磁盘
- 用于测试环境模拟调度系统的文件接收

#### 17.3.1 fggg 版本核心类结构

```
MainWindow (mainwindow.cpp, 2236行)
  ├── Zivid相机管理 (T1/T2 双相机)
  │   ├── updateCameraList() - 枚举连接相机
  │   ├── capture2D3D() - 同步采集2D+3D
  │   └── saveDataThread - 并行保存线程
  ├── TCP服务器 (serverthread.cpp)
  │   ├── Port1:21000 - 接收调度指令
  │   ├── Port2:22000 - 备用端口
  │   ├── 指令解析: START/PHOTO/CHANGE/GAIN
  │   └── 完成回复: <START&SUCCESSED&0!> / <PHOTO&T1&SUCCESSED&0!>
  ├── HTTP上传线程 (httpthread.cpp)
  │   └── multipart/form-data POST to 192.168.0.201:8080/upload
  ├── 深度图处理
  │   ├── float2short() - NaN→-32768
  │   └── BigEndian 二进制格式保存
  ├── 数据管理
  │   ├── 按时间戳创建目录 ../data/yyyyMMdd_HHmmss/
  │   ├── autodelete.txt - 自动清理配置
  │   └── log/ - 运行日志
  └── 试用期管理
      └── checkTrialExpiration() - 2024-12-30截止
```

#### 17.2.1 验收材料 (`南昌项目验收材料26.4.15/`)

共13个子目录：

| 目录 | 内容 |
|------|------|
| `6现场施工图纸及施工说明书/` | 轨道施工图、二维码支架作业指导书 |
| `7设计方案说明书/` | 总体设计方案 |
| `8系统原理、接线图/` | 2个DWG电气图纸 |
| `9主要零部件结构图/` | 1个DWG结构图 |
| `10设备装配图/` | 装配图纸 |
| `11操作使用手册/` | 2份操作手册 |
| `12外购件清单/` | 易损易耗件清单 |
| `13日常维护保养手册/` | 2份保养手册 + 点检表 |
| `14备品备件清单/` | 备件清单及说明 |
| `15培训签到记录/` | 1张培训签到照片 |
| `16发票清单/` | 发票清单 |
| `17合格证/` | 3类合格证（检车机器人/地沟轨道/升降梯） |
| 验收确认报告 | 南昌机辆段验收确认 |

#### 17.2.2 智能检车机器人验收确认报告

南昌机辆段南整备场J20道的智能检车机器人验收确认报告，包含验收项目、标准、结果等。

### 17.3 南宁项目（国智中心方案，本项目）

| 文件 | 内容 |
|------|------|
| `附件3 中国铁路南宁局集团有限公司科技成果示范性推广项目申请表.doc` | 南宁局科技成果推广申请 |
| `南宁地形模拟V2.3.PDF` | 南宁机务段地形3D模拟图 |

### 17.4 项目组织实施指导书要点

《机车巡检机器人项目组织实施指导书》涵盖8大章节：

| 章节 | 内容 |
|------|------|
| 一、项目概述 | 背景、目标、范围、现场确认 |
| 二、设备技术要求 | 轨道模型、设备用途、安装条件、主要技术参数 |
| 三、项目协议 | 双方职责、一键巡检/一般巡检功能、算法系统、接口/安全性要求 |
| 四、施工方案 | 施工概况、材料工具、施工要求（轨道/二维码/综合控制柜/升降平台/安全警示/定位检测/WiFi AP/网络接口/预埋管路） |
| 五、设备软件部署 | 机器人控制（组网/路径规划/定位/通讯）、服务端软件（调度/算法/安全/通讯/视频/网络/算法部署/指标） |
| 六、网络部分 | 网络设备安装、交换机/VPN路由器/无线控制器配置 |
| 七、采集算法部署 | 零部件位置标定、算法集成 |
| 八、机器人定位标定 | RFID定位/遥控/控制柜操作、运动控制（归零/定位/示教/标定）、采集协议 |

---

> **备查：** 所有原始资料文件保存在 `C:\Users\Administrator\Desktop\机车巡检机器人\资料\` 目录下。源码文件保存在 `C:\Users\Administrator\Desktop\机车巡检机器人\机车巡检机器人\Collector_Manage\` 目录下。开发对话记录保存在 `C:\Users\Administrator\Desktop\机车巡检机器人\Qwen历史对话信息\` 目录下。
